services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    networks:
      - frontend
  db:
    image: postgres:alpine
    environment:
      POSTGRES_PASSWORD: password123
    networks:
      - backend

networks:
  frontend:
  backend:
